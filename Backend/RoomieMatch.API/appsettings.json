{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "AppProgDb": "Host=127.0.0.1;Port=5432;Database=RoomieMatchDb;Username=postgres;Password=postgres"
  },
  "AppSettings": {
    "Token": "this is a super secret key that is definitely long enough to be secure and satisfy the hmacsha512 requirements which requires at least 64 bytes of data to be secure."
  }
}